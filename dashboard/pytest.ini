[pytest]
testpaths = tests
python_files = test_*.py
addopts = -ra
markers =
    slow: touches the network or spawns subprocesses; deselect with -m "not slow"
    allow_network: opt out of the hermetic network block (conftest._no_live_network)
